# Copy to a deployment-only environment file outside Git.
# Use a long, unique password generated by a password manager.
NETGUARD_USERNAME=operator
NETGUARD_PASSWORD=replace-with-a-long-random-password
NETGUARD_ALLOWED_HOSTS=localhost,127.0.0.1,[::1]
